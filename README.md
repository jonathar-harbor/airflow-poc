# Getting Started

```bash
cp .envrc.sample .envrc
direnv allow
make init up
```

In your browser visit: `http://localhost:8080`

Your credentials are:
- *username*: airflow
- *password*: airlfow

If you have questions about what the make targets are and what they do run:

```bash
make help
```
